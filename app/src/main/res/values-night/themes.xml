<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrayerTimes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/offWhite</item>
        <item name="colorPrimaryDark">@color/DarkGreen</item>

        <item name="android:statusBarColor">@color/DarkGreen</item>
        <item name="statusBarBackground">@color/DarkGreen</item>
    </style>
</resources>